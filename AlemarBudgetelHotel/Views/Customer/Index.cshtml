@{
    ViewData["Title"] = "Alemar Budgetel Hotel";
    Layout = "_Layout";
}

@section Styles {
    <style>
        /* ── Remove the top padding the layout's .site-main might add ── */
        .site-main {
            padding: 0 !important;
        }

        /* ─────────────── HERO ─────────────── */
        #home {
            min-height: 100vh;
            position: relative;
            display: flex;
            align-items: center;
            overflow: hidden;
        }

        .hero-bg {
            position: absolute;
            inset: 0;
            background: linear-gradient(135deg, rgba(10,10,10,0.85) 0%, rgba(10,10,10,0.4) 50%, rgba(10,10,10,0.8) 100%), url('https://images.unsplash.com/photo-1566073771259-6a8506099945?w=1920&q=80') center/cover no-repeat;
        }

            .hero-bg::after {
                content: '';
                position: absolute;
                bottom: 0;
                left: 0;
                right: 0;
                height: 200px;
                background: linear-gradient(to top, var(--dark-2), transparent);
            }

        .hero-content {
            position: relative;
            z-index: 2;
            padding: 0 60px;
            max-width: 750px;
        }

        .hero-tag {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            font-size: 0.75rem;
            font-weight: 500;
            letter-spacing: 4px;
            text-transform: uppercase;
            color: var(--gold);
            margin-bottom: 24px;
            opacity: 0;
            animation: fadeUp 0.8s ease 0.2s forwards;
        }

            .hero-tag::before, .hero-tag::after {
                content: '';
                width: 30px;
                height: 1px;
                background: var(--gold);
            }

        .hero-title {
            font-family: 'Playfair Display', serif;
            font-size: clamp(3rem, 7vw, 5.5rem);
            font-weight: 900;
            line-height: 1.05;
            color: var(--white);
            margin-bottom: 24px;
            opacity: 0;
            animation: fadeUp 0.8s ease 0.4s forwards;
        }

            .hero-title em {
                font-style: italic;
                color: var(--gold);
            }

        .hero-desc {
            font-size: 1.1rem;
            font-weight: 300;
            color: rgba(245,240,232,0.75);
            line-height: 1.8;
            margin-bottom: 40px;
            max-width: 500px;
            opacity: 0;
            animation: fadeUp 0.8s ease 0.6s forwards;
        }

        .hero-btns {
            display: flex;
            gap: 16px;
            align-items: center;
            flex-wrap: wrap;
            opacity: 0;
            animation: fadeUp 0.8s ease 0.8s forwards;
        }

        .btn-hero-primary {
            background: var(--gold);
            color: var(--dark);
            padding: 16px 40px;
            font-family: 'Jost', sans-serif;
            font-size: 0.85rem;
            font-weight: 600;
            letter-spacing: 2px;
            text-transform: uppercase;
            text-decoration: none;
            border: none;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            transition: all 0.3s ease;
        }

            .btn-hero-primary:hover {
                background: var(--gold-light);
                color: var(--dark);
                transform: translateY(-2px);
                box-shadow: 0 10px 40px rgba(201,168,76,0.3);
            }

        .btn-hero-outline {
            background: transparent;
            color: var(--cream);
            padding: 15px 40px;
            font-family: 'Jost', sans-serif;
            font-size: 0.85rem;
            font-weight: 500;
            letter-spacing: 2px;
            text-transform: uppercase;
            text-decoration: none;
            border: 1px solid rgba(245,240,232,0.4);
            transition: all 0.3s ease;
        }

            .btn-hero-outline:hover {
                border-color: var(--gold);
                color: var(--gold);
            }

        .hero-stats {
            position: absolute;
            bottom: 60px;
            right: 60px;
            display: flex;
            gap: 50px;
            z-index: 2;
            opacity: 0;
            animation: fadeUp 0.8s ease 1s forwards;
        }

        .stat-num {
            font-family: 'Playfair Display', serif;
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--gold);
            line-height: 1;
            text-align: center;
        }

        .stat-label {
            font-size: 0.7rem;
            letter-spacing: 3px;
            text-transform: uppercase;
            color: rgba(245,240,232,0.6);
            margin-top: 6px;
            text-align: center;
        }

        /* ─────────────── ROOMS SECTION ─────────────── */
        #rooms {
            padding: 120px 60px;
            background: var(--dark-3);
        }

        .rooms-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-end;
            margin-bottom: 60px;
            flex-wrap: wrap;
            gap: 30px;
        }

        .section-title {
            font-family: 'Playfair Display', serif;
            font-size: clamp(2rem, 4vw, 3rem);
            font-weight: 700;
            color: var(--white);
            line-height: 1.2;
            margin-bottom: 0;
        }

        .section-desc {
            font-size: 1rem;
            font-weight: 300;
            color: var(--gray);
            line-height: 1.8;
            max-width: 500px;
        }

        .rooms-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 2px;
        }

        .room-card {
            position: relative;
            overflow: hidden;
            aspect-ratio: 3/4;
            cursor: pointer;
            background: var(--dark-3);
        }

            .room-card:first-child {
                grid-row: span 2;
                aspect-ratio: auto;
            }

        .room-img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.6s ease;
            filter: brightness(0.7);
        }

        .room-card:hover .room-img {
            transform: scale(1.05);
            filter: brightness(0.45);
        }

        .room-overlay {
            position: absolute;
            inset: 0;
            background: linear-gradient(to top, rgba(0,0,0,0.9) 0%, rgba(0,0,0,0.2) 50%, transparent 100%);
            display: flex;
            flex-direction: column;
            justify-content: flex-end;
            padding: 30px;
        }

        .room-type {
            font-size: 0.65rem;
            letter-spacing: 4px;
            text-transform: uppercase;
            color: var(--gold);
            margin-bottom: 8px;
        }

        .room-name {
            font-family: 'Playfair Display', serif;
            font-size: 1.4rem;
            font-weight: 700;
            color: var(--white);
            margin-bottom: 8px;
        }

        .room-price {
            font-size: 0.9rem;
            color: rgba(245,240,232,0.7);
        }

            .room-price strong {
                color: var(--gold);
                font-size: 1.1rem;
            }

        .room-btn {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) scale(0.8);
            background: var(--gold);
            color: var(--dark);
            padding: 12px 28px;
            font-size: 0.75rem;
            font-weight: 600;
            letter-spacing: 2px;
            text-transform: uppercase;
            text-decoration: none;
            opacity: 0;
            transition: all 0.3s ease;
            white-space: nowrap;
        }

        .room-card:hover .room-btn {
            opacity: 1;
            transform: translate(-50%, -50%) scale(1);
        }

        /* ─────────────── ABOUT SECTION ─────────────── */
        #about {
            padding: 120px 60px;
            background: var(--dark);
        }

        .about-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 80px;
            align-items: center;
        }

        .about-img-wrap {
            position: relative;
        }

        .about-img {
            width: 100%;
            height: 600px;
            object-fit: cover;
            display: block;
        }

        .about-img-accent {
            position: absolute;
            bottom: -30px;
            right: -30px;
            width: 200px;
            height: 200px;
            border: 2px solid var(--gold);
            z-index: -1;
        }

        .about-badge {
            position: absolute;
            top: 40px;
            right: -20px;
            background: var(--gold);
            color: var(--dark);
            padding: 20px;
            text-align: center;
        }

        .about-badge-num {
            font-family: 'Playfair Display', serif;
            font-size: 2.5rem;
            font-weight: 900;
            line-height: 1;
        }

        .about-badge-text {
            font-size: 0.65rem;
            font-weight: 600;
            letter-spacing: 2px;
            text-transform: uppercase;
            margin-top: 4px;
        }

        .gold-divider {
            width: 60px;
            height: 2px;
            background: var(--gold);
            margin-bottom: 24px;
        }

        .about-features {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 12px;
            margin: 10px 0 0;
        }

        .feature-item {
            display: flex;
            align-items: center;
            gap: 12px;
            font-size: 0.88rem;
            color: var(--cream);
        }

        .feature-icon {
            width: 38px;
            height: 38px;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
            font-size: 0.95rem;
            border-radius: 4px;
        }

        /* Available — gold tint */
        .feature-icon--available {
            background: rgba(201,168,76,0.12);
            border: 1px solid rgba(201,168,76,0.35);
            color: var(--gold);
        }

        /* Not Available — red tint */
        .feature-icon--unavailable {
            background: rgba(220,60,60,0.08);
            border: 1px solid rgba(220,60,60,0.3);
        }

            .feature-icon--unavailable .fa-stack {
                font-size: 0.58em;
                width: 1em;
                height: 1em;
                line-height: 1em;
            }

            .feature-icon--unavailable .fa-stack-1x {
                color: rgba(220,60,60,0.65);
            }

            .feature-icon--unavailable .fa-stack-2x {
                color: rgba(220,60,60,0.9);
            }

        /* Section wrappers */
        .amenities-section {
            margin: 18px 0 0;
        }

        .amenities-section-label {
            display: inline-flex;
            align-items: center;
            gap: 7px;
            font-size: 0.62rem;
            font-weight: 700;
            letter-spacing: 3px;
            text-transform: uppercase;
            padding: 5px 12px;
            border-radius: 2px;
            margin-bottom: 14px;
        }

        .amenities-label-available {
            background: rgba(201,168,76,0.12);
            border: 1px solid rgba(201,168,76,0.35);
            color: var(--gold);
        }

        .amenities-label-unavailable {
            background: rgba(220,60,60,0.08);
            border: 1px solid rgba(220,60,60,0.3);
            color: rgba(220,80,80,0.95);
        }

        /* ─────────────── CONTACT SECTION ─────────────── */
        #contact {
            padding: 120px 60px;
            background: var(--dark-2);
        }

        .contact-header {
            text-align: center;
            margin-bottom: 60px;
        }

            .contact-header .section-tag {
                justify-content: center;
            }

            .contact-header .section-desc {
                margin: 0 auto;
            }

        .contact-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 80px;
            align-items: start;
        }

        .contact-info-item {
            display: flex;
            gap: 20px;
            margin-bottom: 36px;
        }

        .contact-icon {
            width: 50px;
            height: 50px;
            background: rgba(201,168,76,0.1);
            border: 1px solid rgba(201,168,76,0.3);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--gold);
            font-size: 1.1rem;
            flex-shrink: 0;
        }

        .contact-label {
            font-size: 0.7rem;
            letter-spacing: 3px;
            text-transform: uppercase;
            color: var(--gold);
            margin-bottom: 6px;
        }

        .contact-value {
            font-size: 1rem;
            color: var(--cream);
        }

        .contact-form {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 16px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

            .form-group label {
                font-size: 0.7rem;
                letter-spacing: 3px;
                text-transform: uppercase;
                color: var(--gold);
            }

            .form-group input,
            .form-group textarea,
            .form-group select {
                background: rgba(255,255,255,0.04);
                border: 1px solid rgba(255,255,255,0.1);
                color: var(--cream);
                padding: 14px 18px;
                font-family: 'Jost', sans-serif;
                font-size: 0.95rem;
                outline: none;
                transition: border-color 0.3s ease;
                resize: none;
            }

                .form-group input:focus,
                .form-group textarea:focus,
                .form-group select:focus {
                    border-color: var(--gold);
                }

                .form-group input::placeholder,
                .form-group textarea::placeholder {
                    color: rgba(255,255,255,0.25);
                }

                .form-group select option {
                    background: var(--dark-3);
                }

        /* ─────────────── ANIMATIONS ─────────────── */
        @@keyframes fadeUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* ─────────────── RESPONSIVE ─────────────── */
        @@media (max-width: 768px) {
            .hero-content {
                padding: 0 24px;
            }

            .hero-stats {
                right: 24px;
                gap: 24px;
            }

            #rooms, #about, #contact {
                padding: 80px 24px;
            }

            .rooms-grid {
                grid-template-columns: 1fr;
            }

            .room-card:first-child {
                grid-row: span 1;
                aspect-ratio: 3/4;
            }

            .about-grid, .contact-grid {
                grid-template-columns: 1fr;
                gap: 40px;
            }

            .about-img {
                height: 350px;
            }

            .form-row {
                grid-template-columns: 1fr;
            }
        }
    </style>
}

<!-- ═══ HERO ═══ -->
<section id="home">
    <div class="hero-bg"></div>
    <div class="hero-content">
        <div class="hero-tag">Welcome to Luxury</div>
        <h1 class="hero-title">Your Perfect<br><em>Escape</em> Awaits</h1>
        <p class="hero-desc">Experience unparalleled comfort and elegance at Alemar Budgetel Hotel. Where every stay becomes a cherished memory.</p>
        <div class="hero-btns">
            <a asp-controller="Customer" asp-action="Rooms" class="btn-hero-primary">
                <i class="fas fa-calendar-check"></i> Book Your Stay
            </a>
            <a href="#about" class="btn-hero-outline">Discover More</a>
        </div>
    </div>
    <div class="hero-stats">
        <div><div class="stat-num">18</div><div class="stat-label">Rooms</div></div>
        <div><div class="stat-num">6</div><div class="stat-label">Floors</div></div>
        <div><div class="stat-num">24/7</div><div class="stat-label">Service</div></div>
    </div>
</section>

<!-- ═══ ROOMS PREVIEW ═══ -->
<section id="rooms">
    <div class="rooms-header reveal">
        <div>
            <div class="section-tag">Our Accommodations</div>
            <h2 class="section-title">Rooms &amp;<br>Suites</h2>
        </div>
        <div>
            <p class="section-desc">From cozy singles to luxurious super duper suites — find the perfect room for your stay.</p>
            <br>
            <a asp-controller="Customer" asp-action="Rooms" class="btn-gold" style="margin-top:16px;">
                View All Rooms <i class="fas fa-arrow-right"></i>
            </a>
        </div>
    </div>
    <div class="rooms-grid reveal">
        <div class="room-card">
            <img class="room-img" src="https://images.unsplash.com/photo-1618773928121-c32242e63f39?w=800&q=80" alt="Super Duper Suite" />
            <div class="room-overlay">
                <div class="room-type">Featured Suite</div>
                <div class="room-name">Super Duper Suite</div>
                <div class="room-price">From <strong>₱2,000</strong> / 3hrs</div>
            </div>
            <a asp-controller="Customer" asp-action="Rooms" class="room-btn">Book Now</a>
        </div>
        <div class="room-card">
            <img class="room-img" src="https://images.unsplash.com/photo-1631049307264-da0ec9d70304?w=800&q=80" alt="Standard Room" />
            <div class="room-overlay">
                <div class="room-type">Comfort</div>
                <div class="room-name">Standard Room</div>
                <div class="room-price">From <strong>₱700</strong> / 3hrs</div>
            </div>
            <a asp-controller="Customer" asp-action="Rooms" class="room-btn">Book Now</a>
        </div>
        <div class="room-card">
            <img class="room-img" src="https://images.unsplash.com/photo-1590490360182-c33d57733427?w=800&q=80" alt="Double Room" />
            <div class="room-overlay">
                <div class="room-type">Cozy</div>
                <div class="room-name">Double Room</div>
                <div class="room-price">From <strong>₱500</strong> / 3hrs</div>
            </div>
            <a asp-controller="Customer" asp-action="Rooms" class="room-btn">Book Now</a>
        </div>
        <div class="room-card">
            <img class="room-img" src="https://images.unsplash.com/photo-1505692952047-1a78307da8f2?w=800&q=80" alt="Deluxe Room" />
            <div class="room-overlay">
                <div class="room-type">Premium</div>
                <div class="room-name">Deluxe Room</div>
                <div class="room-price">From <strong>₱1,000</strong> / 3hrs</div>
            </div>
            <a asp-controller="Customer" asp-action="Rooms" class="room-btn">Book Now</a>
        </div>
    </div>
</section>

<!-- ═══ ABOUT ═══ -->
<section id="about">
    <div class="about-grid">
        <div class="about-img-wrap reveal">
            <img class="about-img" src="https://images.unsplash.com/photo-1542314831-068cd1dbfeeb?w=800&q=80" alt="About Alemar Budgetel Hotel" />
            <div class="about-img-accent"></div>
            <div class="about-badge">
                <div class="about-badge-num">10+</div>
                <div class="about-badge-text">Years of<br>Excellence</div>
            </div>
        </div>
        <div class="about-content reveal">
            <div class="section-tag">Our Story</div>
            <h2 class="section-title">A Place to Call <em style="font-style:italic;color:var(--gold)">Home</em></h2>
            <div class="gold-divider"></div>
            <p class="section-desc">Alemar Budgetel Hotel has been a beacon of comfort and hospitality for over a decade. Nestled in the heart of the city, we offer an exceptional blend of modern amenities and warm Filipino hospitality.</p>
            <p class="section-desc" style="margin-top:16px;">Our dedicated team ensures every guest feels welcomed, valued, and cared for throughout their stay. Whether you're here for a few hours or a full day, we make every moment count.</p>
            <!-- ── AVAILABLE AMENITIES ── -->
            <div class="amenities-section">
                <div class="amenities-section-label amenities-label-available">
                    <i class="fas fa-check-circle"></i> Available
                </div>
                <div class="about-features">
                    <div class="feature-item">
                        <div class="feature-icon feature-icon--available"><i class="fas fa-wifi"></i></div>
                        Free High-Speed WiFi
                    </div>
                    <div class="feature-item">
                        <div class="feature-icon feature-icon--available"><i class="fas fa-snowflake"></i></div>
                        Air Conditioning
                    </div>
                    <div class="feature-item">
                        <div class="feature-icon feature-icon--available"><i class="fa-solid fa-bowl-rice"></i></div>
                        Breakfast
                    </div>
                    <div class="feature-item">
                        <div class="feature-icon feature-icon--available"><i class="fas fa-shield-alt"></i></div>
                        24/7 Security &amp; Front Desk
                    </div>
                    <div class="feature-item">
                        <div class="feature-icon feature-icon--available"><i class="fas fa-concierge-bell"></i></div>
                        Room Service
                    </div>
                    <div class="feature-item">
                        <div class="feature-icon feature-icon--available"><i class="fa-solid fa-jug-detergent"></i></div>
                        Full-service Laundry
                    </div>
                    <div class="feature-item">
                        <div class="feature-icon feature-icon--available"><i class="fa-solid fa-child"></i></div>
                        Kid-friendly
                    </div>
                    <div class="feature-item">
                        <div class="feature-icon feature-icon--available"><i class="fas fa-mobile-alt"></i></div>
                        GCash Payment
                    </div>
                </div>
            </div>

            <!-- ── NOT AVAILABLE ── -->
            <div class="amenities-section">
                <div class="amenities-section-label amenities-label-unavailable">
                    <i class="fas fa-times-circle"></i> Not Available
                </div>
                <div class="about-features">
                    <div class="feature-item">
                        <div class="feature-icon feature-icon--unavailable">
                            <span class="fa-stack">
                                <i class="fa-solid fa-dumbbell fa-stack-1x"></i>
                                <i class="fas fa-ban fa-stack-2x"></i>
                            </span>
                        </div>
                        Fitness Center
                    </div>
                    <div class="feature-item">
                        <div class="feature-icon feature-icon--unavailable">
                            <span class="fa-stack">
                                <i class="fa-solid fa-spa fa-stack-1x"></i>
                                <i class="fas fa-ban fa-stack-2x"></i>
                            </span>
                        </div>
                        Spa
                    </div>
                    <div class="feature-item">
                        <div class="feature-icon feature-icon--unavailable">
                            <span class="fa-stack">
                                <i class="fa-solid fa-water-ladder fa-stack-1x"></i>
                                <i class="fas fa-ban fa-stack-2x"></i>
                            </span>
                        </div>
                        Pool
                    </div>
                    <div class="feature-item">
                        <div class="feature-icon feature-icon--unavailable">
                            <span class="fa-stack">
                                <i class="fa-solid fa-hot-tub-person fa-stack-1x"></i>
                                <i class="fas fa-ban fa-stack-2x"></i>
                            </span>
                        </div>
                        Hot Tub
                    </div>
                    <div class="feature-item">
                        <div class="feature-icon feature-icon--unavailable">
                            <span class="fa-stack">
                                <i class="fas fa-paw fa-stack-1x"></i>
                                <i class="fas fa-ban fa-stack-2x"></i>
                            </span>
                        </div>
                        Pets Allowed
                    </div>
                </div>
            </div>
            <a asp-controller="Customer" asp-action="Rooms" class="btn-gold" style="margin-top: 28px;">
                <i class="fas fa-calendar-check"></i> Reserve a Room
            </a>
        </div>
    </div>
</section>

<!-- ═══ CONTACT ═══ -->
<section id="contact">
    <div class="contact-header reveal">
        <div class="section-tag">Get in Touch</div>
        <h2 class="section-title">Contact <em style="font-style:italic;color:var(--gold)">Us</em></h2>
        <p class="section-desc">Have questions or need assistance? We're here to help you plan your perfect stay.</p>
    </div>
    <div class="contact-grid">
        <div class="reveal">
            <div class="contact-info-item">
                <div class="contact-icon"><i class="fas fa-map-marker-alt"></i></div>
                <div><div class="contact-label">Location</div><div class="contact-value">123 Main Street, City, Philippines</div></div>
            </div>
            <div class="contact-info-item">
                <div class="contact-icon"><i class="fas fa-phone"></i></div>
                <div><div class="contact-label">Phone</div><div class="contact-value">+63 917 123 4567</div></div>
            </div>
            <div class="contact-info-item">
                <div class="contact-icon"><i class="fas fa-envelope"></i></div>
                <div><div class="contact-label">Email</div><div class="contact-value">info@alemarbudgetel.com</div></div>
            </div>
            <div class="contact-info-item">
                <div class="contact-icon"><i class="fas fa-clock"></i></div>
                <div><div class="contact-label">Front Desk Hours</div><div class="contact-value">Open 24 Hours, 7 Days a Week</div></div>
            </div>
            <div class="contact-info-item">
                <div class="contact-icon"><i class="fas fa-mobile-alt"></i></div>
                <div>
                    <div class="contact-label">GCash Payment</div>
                    <div class="contact-value">0917-123-4567</div>
                    <div class="contact-value" style="font-size:.85rem;color:var(--gray);">Alemar Budgetel Hotel</div>
                </div>
            </div>
        </div>
        <div class="reveal">
            <form class="contact-form" id="contactForm">
                <div class="form-row">
                    <div class="form-group">
                        <label>Your Name</label>
                        <input type="text" placeholder="Juan Dela Cruz" />
                    </div>
                    <div class="form-group">
                        <label>Phone Number</label>
                        <input type="tel" placeholder="09XX XXX XXXX" />
                    </div>
                </div>
                <div class="form-group">
                    <label>Email Address</label>
                    <input type="email" placeholder="juan@email.com" />
                </div>
                <div class="form-group">
                    <label>Subject</label>
                    <select>
                        <option value="">Select a subject</option>
                        <option>Room Inquiry</option>
                        <option>Reservation Question</option>
                        <option>Payment Concern</option>
                        <option>General Inquiry</option>
                        <option>Feedback</option>
                    </select>
                </div>
                <div class="form-group">
                    <label>Message</label>
                    <textarea rows="5" placeholder="How can we help you?"></textarea>
                </div>
                <button type="submit" class="btn-gold" style="width:100%;justify-content:center;">
                    <i class="fas fa-paper-plane"></i> Send Message
                </button>
            </form>
        </div>
    </div>
</section>

@section Scripts {
    <script>
        // Smooth scroll for anchor links on this page
        document.querySelectorAll('a[href^="#"]').forEach(a => {
            a.addEventListener('click', function (e) {
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    e.preventDefault();
                    target.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            });
        });

        // Contact form submit
        document.getElementById('contactForm')?.addEventListener('submit', function (e) {
            e.preventDefault();
            alert('Thank you! We will get back to you shortly.');
            this.reset();
        });
    </script>
}
